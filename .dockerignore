# ------------------------------------------------------------------------------
# 1. Source Control & Git
# ------------------------------------------------------------------------------
.git
.gitignore
.husky

# ------------------------------------------------------------------------------
# 2. Docker Files
# ------------------------------------------------------------------------------
# Keep the build context clean by ignoring Docker config files.
.dockerignore
Dockerfile
docker-compose.yml
docker-compose.*.yml

# ------------------------------------------------------------------------------
# 3. Dependencies & Build Artifacts
# ------------------------------------------------------------------------------
# Do not copy local node_modules; prevents architecture mismatches (Host OS vs. Alpine).
node_modules
# Do not copy local build output; we build from scratch inside Docker.
dist

# ------------------------------------------------------------------------------
# 4. Environment Variables (SECURITY)
# ------------------------------------------------------------------------------
# Never embed secrets in the image! Env files are injected at runtime.
.env
.env.*
!.env.example

# ------------------------------------------------------------------------------
# 5. Tests & Coverage
# ------------------------------------------------------------------------------
coverage
test
# No need to copy unit test files within src.
# We don't need test code in the production image.
**/*.spec.ts
**/*.test.ts

# ------------------------------------------------------------------------------
# 6. Tooling & Configs (Dev-Only)
# ------------------------------------------------------------------------------
# These files are only needed during development (VSCode, Linter, etc.).
# The NestJS build process does not require them (EXCEPT tsconfig.json and nest-cli.json).
.vscode
.idea
.lintstagedrc
eslint.config.mjs
.prettierrc
.prettierignore

# ------------------------------------------------------------------------------
# 7. Documentation & Logs
# ------------------------------------------------------------------------------
# Documentation is not needed in the production image.
README.md
LICENSE
CHANGELOG.md

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ------------------------------------------------------------------------------
# 8. OS Specific
# ------------------------------------------------------------------------------
.DS_Store
Thumbs.db